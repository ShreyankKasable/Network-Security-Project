1. Create project template by executing template.py file
2. Write the code on setup.py file to import local packages
3. Create a conda virtual env, activate it and install the requirements from requirements.txt
   conda create -n Network-Security-Project) python=3.10 -y
   conda activate Network-Security-Project)
   add required modules to requirements.txt
   Do "pip install -r requirements.txt"
4. Do a "pip list" on terminal to make sure you have local packages installed.
